# 공통코어 Rule Master-RD 매핑 마스터 초안

## 1. 목적

이 문서는 `기획agent_가이드라인`의 `Rule Master`를 기준으로, 지원 Agent가 `RD판.xlsx`의 어떤 항목을 수정해야 하는지 관리하기 위한 최종 초안이다.

현재 합의한 핵심 전제는 아래와 같다.

- RD판의 `상품` 컬럼에는 `상품코드`가 들어간다.
- 지원 Agent의 기준키는 `상품코드 + 보험코드`이다.
- RD판 자동화 범위는 `Rule Master` 중심으로 본다.
- `Note Master`와 주석 정보는 RD 직접수정 대상에서 제외한다.
- 주석은 합산코드 또는 별도 제어 영역으로 본다.
- `일반/건강 단일건`과 `간편 단일건`이 같은 경우만 일반값 자동반영한다.
- 두 한도가 다르면 `가입한도금액RD`는 룰검토 대상으로 분기한다.

---

## 2. 운영 원칙

1. 지원 Agent는 `상품코드 + 보험코드`로 RD 대상 행을 찾는다.
2. 매핑표에는 `as-is`와 `to-be`를 항목명으로 분리하지 않고 업무 항목명만 관리한다.
3. 결과 시트의 표시명은 `상품명`보다 `특약명` 기준으로 맞춘다.
4. `as-is`와 `to-be` 비교는 실행 단계에서 처리한다.
5. `일반/건강 단일건`과 `간편 단일건`이 동일할 때만 `가입한도금액RD`를 일반값으로 자동반영한다.
6. 두 한도가 다르면 `가입한도금액RD`는 자동수정하지 않고 `룰검토필요`로 분기한다.
7. 현재 RD값이 `#SPE...` 같은 룰형이면 자동수정하지 않는다.
8. 허용값이 명확한 금액/단위/코드 계열만 자동반영 대상으로 둔다.
9. 템플릿ID 매핑 정확성보다 먼저, `상품코드 + 보험코드` 매핑 정확성을 사람 검토의 1순위 게이트로 둔다.

---

## 3. 마스터 컬럼 구조

| 컬럼명 | 설명 |
|---|---|
| Rule Master 항목명 | 기획 Agent 결과의 기준 항목명 |
| 의미유형 | 금액, 단위, 코드, 배수, 조건, 메모 등 |
| 적용구분 | 일반/건강, 간편, 공통 등 |
| RD판 항목명 | RD판에서 연결되는 한글 항목명 |
| RD 영문ID | RD판 내부 영문 ID |
| 값변환규칙 | Rule Master 값을 RD 허용값으로 바꾸는 방식 |
| 자동반영기본여부 | Y/N |
| 룰값발생시처리 | 자동반영, 룰검토필요, 미반영 등 |
| 변경이력기록여부 | Y/N |
| 비고 | 운영 메모 |

---

## 4. Rule Master -> RD판 매핑표 초안

| Rule Master 항목명 | 의미유형 | 적용구분 | RD판 항목명 | RD 영문ID | 값변환규칙 | 자동반영기본여부 | 룰값발생시처리 | 변경이력기록여부 | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| 일반/건강 단일건 | 금액 | 일반/건강 | 가입한도금액RD | ZZENT_LMT_AMT_ID | `일반/건강 as-is/to-be 동일 시 값~값` | Y | 룰검토필요 | Y | 일반/건강과 간편이 같을 때만 자동반영 |
| 간편 단일건 | 금액 | 간편 | 가입한도금액RD | ZZENT_LMT_AMT_ID | `간편 as-is/to-be 동일 시 값~값` | Y | 룰검토필요 | Y | 일반/건강과 간편 값이 다르면 룰검토 |
| 가입금액단위 | 단위 | 공통 | 가입금액단위RD | ZZISAM_UNIT_D_ID | 원값 유지 또는 단위 코드 변환 | Y | 룰검토필요 | Y | 허용값 표준 필요 |
| 일반/건강 수술유의fc | 코드/조건 | 일반/건강 | 미정 | 미정 | 코드표 정의 필요 | N | 룰검토필요 | Y | 실제 RD 연결 확정 필요 |
| 간편 수술유의fc | 코드/조건 | 간편 | 미정 | 미정 | 코드표 정의 필요 | N | 룰검토필요 | Y | 실제 RD 연결 확정 필요 |
| 인별합산 | 합산기준 | 공통 | 해당 없음 | 해당 없음 | 직접 변환 없음 | N | 미반영 | N | RD가 아니라 합산코드 검토 영역 |
| 적용조건 | 조건식 | 공통 | 해당 없음 | 해당 없음 | 직접 변환 없음 | N | 미반영 | Y | 참고용 |
| 주석ID | 참조키 | 공통 | 해당 없음 | 해당 없음 | 직접 변환 없음 | N | 미반영 | N | RD 자동화 범위 제외 |
| 종료사유 | 상태/이력 | 공통 | 해당 없음 | 해당 없음 | 직접 변환 없음 | N | 미반영 | Y | 변경이력 참고용 |
| 검토메모 | 메모 | 공통 | 해당 없음 | 해당 없음 | 직접 변환 없음 | N | 미반영 | Y | 변경이력 사유 문구 생성용 |
| 초안상태 | 상태 | 공통 | 해당 없음 | 해당 없음 | 직접 변환 없음 | N | 미반영 | N | 내부 초안 관리용 |

---

## 5. 처리 기준

### A. 자동반영 대상

아래 조건을 모두 만족할 때만 RD판 셀을 수정한다.

- `자동반영기본여부 = Y`
- Rule Master에서 `as-is`와 `to-be` 비교 결과 변경이 있음
- `일반/건강 단일건`과 `간편 단일건`이 동일함
- 상품코드 + 보험코드 매핑이 정확함
- RD 허용값이 명확함
- 현재 RD값이 일반값 형태임

예:
- `일반/건강 단일건 = 3000`, `간편 단일건 = 3000` -> `가입한도금액RD = 3000~3000`
- `가입금액단위 -> 가입금액단위RD`

### B. 룰검토 대상

아래 중 하나면 자동수정하지 않는다.

- `일반/건강 단일건`과 `간편 단일건`이 서로 다름
- 현재 RD값이 `#SPE...` 같은 룰형 값임
- 변경 후보값 자체가 룰 검토를 필요로 함
- 허용값 정의가 불명확함

처리 방식:

- RD판: `기존룰 > 룰검토필요(일반/건강=xxx, 간편=yyy)` 또는 `기존룰 > 룰검토필요(변경값)` 표기
- 색상: 일반 자동반영과 다른 색상 사용
- 변경이력: 검토 가이드 문구 생성

### C. RD판 반영 제외 대상

아래 항목은 RD판 직접수정 대상이 아니다.

- 인별합산
- 주석ID
- Note Master 관련 정보
- 합산기준/연계조건/예외조건 관련 설명 정보

이 항목들은 필요 시 별도 합산코드 검토 또는 운영 검토 영역으로 분리한다.

---

## 6. 값 변환 규칙 초안

| Rule Master 값 예시 | RD판 항목 | 변환 결과 예시 | 처리 |
|---|---|---|---|
| `3000` / `3000` | 가입한도금액RD | `3000~3000` | 자동반영 |
| `2000` / `2500` | 가입한도금액RD | 자동반영 금지 | 룰검토필요 |
| `1000` | 가입금액단위RD | `1000` | 자동반영 |
| `#SPE7Q3211` | 가입한도금액RD | 자동변환 금지 | 룰검토필요 |
| `00` | 코드성 RD | `00` | 허용값 정의 후 적용 |

---

## 7. 변경이력 생성 기준

| 상황 | 변경이력 처리 |
|---|---|
| 일반값 자동반영 | 변경 전값, 변경 후값, 처리결과=`자동반영` |
| 단위 유지 | 변경 전값=변경 후값, 처리결과=`유지` |
| 룰형 값 | 변경 후값 대신 `검토 가이드`, 처리결과=`룰검토필요` |
| RD판 반영 제외 항목 | 기록 생략 또는 별도 검토표로 분리 |
| 매핑 실패 | 처리결과=`미반영`, 사유=`상품코드+보험코드 또는 RD 매핑 실패` |

---

## 8. 샘플 적용 기준

### 사례 1. 암진단

| 항목 | 처리 |
|---|---|
| Rule Master 값 | `일반/건강 단일건: as-is 2000, to-be 3000 / 간편 단일건: as-is 2000, to-be 3000` |
| RD판 매핑 | `가입한도금액RD` |
| 반영 결과 | `1000~2000 > 3000~3000` 같은 일반값 반영 |
| 처리결과 | 자동반영 후보 |

### 사례 2. 소액암진단

| 항목 | 처리 |
|---|---|
| Rule Master 값 | `일반/건강 단일건: as-is 2000, to-be 2000 / 간편 단일건: as-is 2000, to-be 2500` |
| RD판 현재값 | 일반값 또는 룰형 가능 |
| 반영 결과 | `룰검토필요(일반/건강=2000, 간편=2500)` |
| 처리결과 | 룰검토필요 |

---

## 9. 도입 순서

1. `일반/건강 단일건`, `간편 단일건`, `가입금액단위` 3개부터 고정 매핑
2. 상품코드 + 보험코드 기준으로 RD 대상 행 탐색
3. 일반/간편 한도가 같을 때만 자동반영
4. 일반/간편 한도가 다를 때는 룰검토필요로 분기
5. 룰형 값은 자동수정 없이 검토가이드만 생성
6. 이후 코드성 RD와 배수성 RD를 순차 확장

---

## 10. 내 의견

지금은 `상품코드 + 보험코드` 단일 매핑 구조로 단순화하는 것이 맞다.

특히 주석과 합산 관련 정보는 RD 자동화에서 억지로 같이 처리하지 않는 편이 안정적이다.  
지원 Agent는 우선 `값형 RD 자동반영 + 룰형 RD 검토분리 + 변경이력 생성` 역할에 집중하는 것이 가장 현실적이다.
