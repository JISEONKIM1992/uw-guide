# 지원 Agent 최종 정리

## 1. 역할

지원 Agent는 `기획agent_가이드라인`의 `Rule Master`를 받아 RD판을 수정한다.

역할 범위는 아래와 같다.

- `상품코드 + 보험코드`로 RD 대상 행 식별
- 결과 시트 표시는 `상품명`보다 `특약명` 기준으로 관리
- `일반/건강 단일건`과 `간편 단일건` 비교
- 동일하면 `가입한도금액RD` 일반값 자동반영
- 다르면 `가입한도금액RD` 룰검토필요로 분기
- `가입금액단위RD`는 값이 있으면 유지 또는 코드변환
- 변경이력 시트 생성
- `변경히스토리`, `지원Agent_수정필요항목`, `지원Agent_1장요약`은 전체 특약을 순회해 생성

---

## 2. 기준키

- `RD판 상품 컬럼 = 상품코드`
- `RD판 보험코드 컬럼 = 보험코드`
- 기준키는 `상품코드 + 보험코드`

---

## 3. 자동반영 규칙

### 가입한도금액RD

1. `일반/건강 단일건 == 간편 단일건`
2. RD 현재값이 일반값이면 `기존값 > 변경값~변경값`
3. 노란색으로 강조
4. 변경이력에 `자동반영` 기록

### 룰검토 분기

1. `일반/건강 단일건 != 간편 단일건`
2. RD 현재값이 `#SPE...` 같은 룰형
3. `기존룰 > 룰검토필요(일반/건강=xxx, 간편=yyy)`로 표기
4. 노란색이 아닌 별도 색으로 강조
5. 변경이력에 `룰검토필요` 기록

### 가입금액단위RD

- 값이 없으면 건드리지 않음
- 값이 있으면 유지
- 변경이력에는 `유지`로 남김

---

## 4. 제외 범위

- `Note Master`
- 주석ID
- 인별합산
- 합산코드 검토 영역

이 항목들은 RD 직접수정보다 별도 검토 영역으로 본다.

---

## 5. 출력물

지원 Agent는 다음 산출물을 만든다.

- RD 수정본
- `변경히스토리` 시트
- 필요 시 `지원Agent_수정필요항목` 시트
- 필요 시 `지원Agent_1장요약` 시트

---

## 6. 현재 파일 기준 예시

### 자동반영 예시

- `암진단 / LI00112`
- 일반/건강 3000, 간편 3000
- `가입한도금액RD = 1000~2000 > 3000~3000`
- `신입원특약 / LI00120`
- 일반/건강 3000, 간편 4000
- `가입한도금액RD = 3000~3000 > 3000~4000`

### 룰검토 예시

- `소액암진단 / LI00113`
- 일반/건강 2000, 간편 2500
- `가입한도금액RD = 1000~1000 > 룰검토필요(일반/건강=2000, 간편=2500)`

---

## 7. 운영 메모

지원 Agent는 처음부터 모든 RD를 자동반영하려고 하지 않고, 금액/단위처럼 허용값이 명확한 항목부터 좁게 시작하는 것이 안전하다.

룰형 값은 자동수정하지 않고, 검토 사유를 남기는 쪽이 현업 운영에 더 맞다.
