# 공통코어-RD 2트랙 매핑 초안

## 1. 목적

이 문서는 `신계약가이드라인_공통코어_샘플결과`를 기준으로 지원 Agent가 RD판을 반영할 때,

- `Rule Master` 중심의 값형 항목
- `Note Master` 중심의 주석형 항목

을 분리해서 매핑하기 위한 초안이다.

핵심 방향은 다음과 같다.

1. 값형 항목은 가능하면 RD 직접반영 대상으로 관리한다.
2. 주석형 항목은 처음부터 RD 직접수정보다 `영향영역 매핑 + 검토 가이드 생성` 중심으로 관리한다.
3. 룰 여부는 RD 종류로 미리 단정하지 않고, 실제 값 처리 시점에 분기한다.

---

## 2. 전체 구조

### Track A. Rule Master 기준 값형 매핑

- 대상: 단일건, 가입금액단위, 코드성 값, 배수성 값
- 목적: RD 셀 자동반영 또는 자동반영 후보 생성
- 처리 방식: `공통코어 항목 -> RD판 항목` 직접 매핑

### Track B. Note Master 기준 주석형 매핑

- 대상: 가입조건, 연계조건, 예외/상향기준, 합산기준, 가입제한
- 목적: RD 직접수정보다 검토 포인트와 가이드 문구 생성
- 처리 방식: `주석유형 -> 영향 RD/영역 -> 검토처리구분` 매핑

---

## 3. Track A. Rule Master 기준 값형 매핑표 초안

| 공통코어 기준 항목 | 원천 시트 | 의미유형 | RD판 항목명 | RD 영문ID | 값 변환 규칙 | 기본 처리 | 룰값 발생 시 처리 | 비고 |
|---|---|---|---|---|---|---|---|---|
| as-is 일반/건강 단일건 | Rule Master | 비교기준값 | 가입한도금액RD | ZZENT_LMT_AMT_ID | 변경 전 비교용 | 이력기록용 | 룰 비교기준 유지 | 직접반영 아님 |
| to-be 일반/건강 단일건 | Rule Master | 금액 | 가입한도금액RD | ZZENT_LMT_AMT_ID | `값 -> 값~값` | 자동반영 | 자동수정 금지 + 룰검토가이드 | 우선 자동화 대상 |
| as-is 간편 단일건 | Rule Master | 비교기준값 | 가입한도금액RD | ZZENT_LMT_AMT_ID | 변경 전 비교용 | 이력기록용 | 룰 비교기준 유지 | 직접반영 아님 |
| to-be 간편 단일건 | Rule Master | 금액 | 가입한도금액RD | ZZENT_LMT_AMT_ID | `값 -> 값~값` | 자동반영 후보 | 자동수정 금지 + 룰검토가이드 | 템플릿ID 분기 후 적용 |
| 가입금액단위 | Rule Master 또는 별도 입력항목 | 단위 | 가입금액단위RD | ZZISAM_UNIT_D_ID | 원값 유지 또는 코드 변환 | 자동반영 | 자동수정 금지 + 검토표기 | 단위 코드표 필요 |
| 일반/건강 수술유의fc | Rule Master | 코드/조건 | 미정 | 미정 | 코드표 필요 | 검토대상 | 룰검토가이드 | 실제 RD 연결 확정 필요 |
| 간편 수술유의fc | Rule Master | 코드/조건 | 미정 | 미정 | 코드표 필요 | 검토대상 | 룰검토가이드 | 실제 RD 연결 확정 필요 |
| 인별합산 | Rule Master | 합산기준 | 미정 | 미정 | 직접 변환 없음 | 검토대상 | 룰검토가이드 | RD 직접반영보다 합산 검토 가능성 큼 |
| 적용조건 | Rule Master | 조건식 | 미정 | 미정 | 직접 변환 없음 | 검토대상 | 룰검토가이드 | 템플릿ID 분기/룰 검토 참고 |
| 주석ID | Rule Master | 참조키 | Note Master 연결 | 없음 | Note Master 참조 | 주석트랙 전달 | 주석트랙 전달 | 직접 RD 반영 아님 |
| 검토메모 | Rule Master | 메모 | 변경히스토리 시트 | 없음 | 원문 유지 | 이력반영 | 이력반영 | 검토사유 생성에 활용 |

### Track A 처리 기준

| 조건 | 처리 방식 |
|---|---|
| 값형 + 허용값 명확 + 템플릿ID 매핑 완료 | RD판 자동반영 |
| 값형 + RD 현재값이 룰형(`#SPE...`) | 자동수정 금지, 룰검토필요 표기 |
| 값형 + RD 허용값 불명확 | 자동반영 금지, 검토대상 |
| 비교값만 존재 | 변경이력 생성만 수행 |

---

## 4. Track B. Note Master 기준 주석형 매핑표 초안

| 주석유형 | 주석 예시 성격 | 우선 처리구분 | 주요 영향 RD/영역 | 자동반영 여부 | 생성 산출물 | 비고 |
|---|---|---|---|---|---|---|
| 가입조건 | 일정 금액 초과 시 조건 부여 | 검토메모/조건검토 | 조건성 RD, 심사 조건 영역 | N | 변경히스토리 메모, 검토 체크리스트 | 직접 RD 수정보다 조건검토 우선 |
| 연계조건 | A 가입 시 B 필수 | 룰 검토 | 특약관계전산심사코드RD, 연계심사 영역 | N | 룰검토가이드 | 관계형 영향 가능 |
| 예외/상향기준 | 연령/조건별 예외 한도 | 룰 검토 | 가입한도금액RD 관련 룰, 예외조건 영역 | N | 룰검토가이드 | 값형보다 룰/예외 검토가 핵심 |
| 합산기준 | 인별합산/묶음특약 합산 | 합산 검토 | 인별합산, 기계약합산코드, 합산 로직 영역 | N | 합산 검토메모 | RD 직접수정보다 로직 점검 가능성 큼 |
| 위험등급 조건 | 등급별 차등 한도 | 룰 검토 | 가입한도금액RD 룰, 위험등급 로직 | N | 룰검토가이드 | 등급 파라미터 확인 필요 |
| 가입제한 | 타사 가입 시 불가 등 | 룰 검토 | 가입제한 룰, 심사제한 영역 | N | 룰검토가이드 | 직접 값변경보다 제한로직 확인 |

### Track B 처리 기준

| 조건 | 처리 방식 |
|---|---|
| 주석유형이 연계조건/가입제한/예외기준 | RD 직접수정 없이 룰검토가이드 생성 |
| 주석유형이 합산기준 | 합산 검토메모 생성 |
| 주석유형이 가입조건 | 조건검토 메모 생성 |
| 주석유형이 특정 RD와 1:1 확정되지 않음 | 직접반영 금지 |

---

## 5. 지원 Agent 적용 흐름 초안

### 1단계. Rule Master 처리

- 시행예정 여부 확인
- 변경 전/후 값 비교
- 템플릿ID 매핑
- 값형 항목이면 RD판 직접반영 후보 생성

### 2단계. Note Master 처리

- Rule ID와 연결된 주석ID 확인
- Note Master에서 주석유형 조회
- 주석유형별 검토 가이드 생성

### 3단계. RD 처리 분기

- 일반값: `기존값 > 변경값` 형식 + 노란색
- 룰값: `기존룰 > 룰검토필요(변경값)` 형식 + 별도 색
- 주석형: RD 직접수정 없이 변경히스토리/검토시트에 기록

### 4단계. 변경이력 생성

- 어떤 Rule Master 항목이 어떤 RD판 항목에 반영됐는지 기록
- 어떤 Note Master 주석유형이 어떤 검토 가이드를 만들었는지 기록

---

## 6. 샘플 적용 이미지

### 사례 1. 소액암진단 단일건 상향

| 구분 | 처리 |
|---|---|
| Rule Master | `to-be 일반/건강 단일건 = 2000` |
| RD판 매핑 | `가입한도금액RD` |
| 반영 방식 | `1000~1000 > 2000~2000` |
| 주석 연계 | `연계조건`, `예외/상향기준` |
| 주석 처리 | 변경이력에 검토가이드 생성 |

### 사례 2. 입원간병인 룰형 한도 변경

| 구분 | 처리 |
|---|---|
| Rule Master | `to-be 일반/건강 단일건 = 3000` |
| RD판 현재값 | `#SPE7Q3211` |
| 반영 방식 | 자동수정 금지 |
| RD 표기 | `#SPE7Q3211 > 룰검토필요(3000)` |
| 주석 처리 | 필요 시 `가입제한` 검토가이드 추가 |

---

## 7. 지금 단계에서 추천하는 운영 방식

1. `공통코어 항목 -> RD판 항목` 직접 매핑표부터 확정
2. 별도로 `주석유형 -> 영향영역/검토가이드` 표를 만든다
3. 값형과 주석형을 섞지 않는다
4. 룰 여부는 매핑표 속성이 아니라 실행 시점 분기 로직으로 처리한다
5. 변경이력 시트에는 값형 반영 결과와 주석형 검토가이드를 함께 보여준다

---

## 8. 의사결정 포인트

이 초안을 실제 운영안으로 바꾸려면 아래를 추가로 정해야 한다.

| 결정 항목 | 현재 상태 |
|---|---|
| 수술유의fc의 실제 RD판 연결 대상 | 미정 |
| 인별합산이 직접 RD인지 검토 메모인지 | 미정 |
| 가입금액단위 허용값 표준 | 미정 |
| 주석유형별 검토가이드 문구 템플릿 | 미정 |
| 변경히스토리 외 별도 검토체크리스트 시트 필요 여부 | 미정 |

---

## 9. 내 의견

현재 구조에서는 `Rule Master = 직접 반영 후보`, `Note Master = 검토 가이드 후보`로 나누는 것이 가장 안전하다.

특히 주석을 바로 RD에 꽂으려 하기보다, 주석유형 기준으로 영향영역을 먼저 잡는 접근이 더 현실적이다.  
이렇게 해야 나중에 템플릿ID 매핑 마스터, RD 허용값 마스터, 룰 검토가이드 템플릿까지 각각 분리해서 확장하기 쉽다.
